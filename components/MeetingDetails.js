export class Meeting {
    constructor(MeetingName, Room_no, Link, Date, Time, Mode, id) {
        this.MeetingName = MeetingName,
            this.Room_no = Room_no,
            this.Link = Link,
            this.Date = Date,
            this.Time = Time,
            this.Mode = Mode,
            this.id = id;
    }
}